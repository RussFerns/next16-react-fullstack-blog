    // // prisma.config.ts
    // import 'dotenv/config'; // Loads environment variables from .env files
    // import type { PrismaConfig } from 'prisma';

    // const config: PrismaConfig = {
    //   datasource: {
    //     // url: process.env.DATABASE_URL, // Or directly the URL string
    //     // ... other datasource configurations like shadowDatabaseUrl, directUrl
    //     url: process.env.POSTGRES_PRISMA_URL, // uses connection pooling
    //     // directUrl: process.env.POSTGRES_URL_NON_POOLING // uses a direct connection
    //   },
    //   // ... other Prisma configurations
    // };

    // export default config;



